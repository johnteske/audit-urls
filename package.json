{
  "name": "audit-urls",
  "version": "0.0.1",
  "description": "audit url statuses, including https availability",
  "main": "dist/index.js",
  "bin": {
    "check-urls": "./dist/cli.js"
  },
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "build": "tsc src/*.ts --declaration --outDir dist && chmod +x dist/cli.js",
    "prepare": "npm run build",
    "prepublishOnly": "npm test",
    "format": "prettier src/*.ts test.js --write",
    "test": "tape test.js"
  },
  "keywords": [],
  "author": "John Teske",
  "license": "MIT",
  "dependencies": {
    "got": "^11.1.3"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "tape": "^5.0.0",
    "tape-promise": "^4.0.0",
    "typescript": "^3.9.2"
  }
}
